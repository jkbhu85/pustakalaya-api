package com.jk.pustakalaya.login;

public class LoginServiceImpl implements LoginService {

	@Override
	public String login(LoginCredentials loginCred) {
		//"jiten".equals(loginCred.getUsername()) && "pwd".equals(loginCred.getPassword())

		return null;
	}

}
